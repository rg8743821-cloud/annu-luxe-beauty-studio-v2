# Images

This project currently uses hosted Unsplash placeholder images directly in
the HTML (per Part 1 requirements) so no local files are needed to preview
the site.

When real studio photography is ready, drop files here (e.g. hero.jpg,
gallery-1.jpg) and update the corresponding `src` attributes across
`public/index.html` and the files in `pages/`.
